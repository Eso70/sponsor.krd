import {
  LINKTREE_DEFAULT_FOOTER_PHONE,
  LINKTREE_DEFAULT_FOOTER_TEXT,
} from "@linktree/types";

export const SPONSOR_TEXT = "سپۆنسەر کراوە لەلایەن";

// Re-exported from the shared contract so the public footer, the link editor
// and the server-side default-page seeder cannot drift apart.
export const DEFAULT_FOOTER_NAME = LINKTREE_DEFAULT_FOOTER_TEXT;
export const DEFAULT_FOOTER_PHONE = LINKTREE_DEFAULT_FOOTER_PHONE;
