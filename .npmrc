confirmModulesPurge=false

# Make the root package.json "engines" field a hard requirement instead of a
# suggestion, so an unsupported Node or pnpm fails at install with a clear
# message rather than at runtime with an obscure one.
engine-strict=true
